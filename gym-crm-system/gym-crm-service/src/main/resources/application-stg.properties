spring.datasource.url=${STG_DB_URL}
spring.datasource.username=${STG_DB_USERNAME}
spring.datasource.password=${STG_DB_PASSWORD}
hibernate.show_sql=false
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
spring.activemq.broker-url=${STG_ACTIVEMQ_BROKER_URL}
spring.activemq.user=${STG_ACTIVEMQ_USER}
spring.activemq.password=${STG_ACTIVEMQ_PASSWORD}
